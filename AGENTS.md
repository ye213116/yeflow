# PaiAgent MVP - 智能体核心工作准则

> [!IMPORTANT]
> 本文件为本仓库最高优先级协作指令。  
> 在本仓库内工作时，必须优先遵循本文件；如与临时提示冲突，以本文件为准，除非开发者明确说明覆盖某条规则。  
> 要求：保持克制，禁止自作主张，严格计划先行。

---

## 1. 项目目标与 MVP 边界

### 1.1 项目目标

PaiAgent 是一个面向 MVP 的智能体工作流产品，当前阶段目标是：

- 提供最小可运行的前后端闭环
- 支持基础工作流定义、保存、加载、运行
- 支持有限节点类型的串行执行
- 支持运行状态追踪与基础调试能力
- 支持最小可替换的 LLM / TTS Provider 抽象
- 支持基本验收与演示

### 1.2 当前阶段明确不做

以下能力在 MVP 阶段默认视为**超范围**，除非开发者明确要求：

- 复杂分布式调度
- 分布式事务
- 复杂工作流编排引擎
- Kafka / Temporal / Camunda / MQ 等重量级基础设施
- 多租户体系
- 复杂 RBAC 权限模型
- 高可用、弹性扩缩、生产级监控告警体系
- 高复杂度插件系统
- 过早抽象的通用平台能力

### 1.3 默认设计原则

- 优先可运行，再谈可扩展
- 优先清晰直接，再谈抽象优雅
- 优先单体内聚，再谈分布式拆分
- 优先满足当前验收场景，再谈未来想象空间

---

## 2. 核心技术栈与强制命令

### 2.1 后端

- 严格基于 **Java 21**
- 严格基于 **Spring Boot 3**
- 必须使用 `./mvnw` 执行所有 Maven 操作
- 绝不能使用系统全局 `mvn`

### 2.2 前端

- 严格基于 **React 18+**
- 严格基于 **TypeScript**
- 严格基于 **Vite**
- 包管理器强制使用 `pnpm`
- 绝不能使用 `npm` 或 `yarn`

### 2.3 环境假定

- 本项目是无历史包袱的 **Greenfield** 项目
- 采用现代最佳实践
- 默认无需考虑向下兼容
- 默认无需为未来假想需求提前做复杂设计
- 本项目当前没有任何真实用户，数据随时可以清空重来，无需考虑数据库平滑迁移或灰度发布逻辑。

---

## 3. 协作模式：强制计划先行（Plan Mode）

### 3.1 复杂任务禁止直接开写

对于以下任务，**绝不能**在收到需求后直接输出大量代码：

- 新增业务节点
- 修改流图逻辑
- 修改执行引擎
- 调整前后端交互契约
- 重构 Provider 抽象
- 新增表结构 / 变更核心状态机
- 调整运行时数据模型
- 引入新模块或新的目录层级

### 3.2 必须先输出《技术实施步骤》

开始编码前，必须先输出一份 Markdown 格式的《技术实施步骤》，至少包含：

1. 目标与范围
2. 数据表 / 状态变更
3. 涉及修改的文件列表
4. 核心逻辑思路
5. 风险点
6. 验证方式
7. 本阶段不做什么

只有在开发者回复 **“同意”** 或 **“继续”** 后，才能开始编码。

### 3.3 单阶段推进

- 每次只专注完成一个阶段
- 未经确认，绝不能把多个阶段揉在一次提交里
- 如果任务明显过大，必须主动拆阶段，而不是一次性做完

---

## 4. 严格边界与绝对禁令

### 4.1 文件与系统操作禁令

- 🚫 绝不能使用 `rm -rf` 或任何破坏性的文件删除命令
- 🚫 绝不能擅自大规模移动、重命名目录结构
- 🚫 绝不能在没有说明的情况下批量格式化整个仓库

### 4.2 版本控制禁令


- 🚫 绝不能自行git创建提交、切分支、rebase、merge、reset、checkout

### 4.3 测试与验证禁令

- 🚫 绝不能自行运行完整单元测试套件
- 🚫 绝不能谎称“测试已通过”
- ✅ 只能给出建议执行的验证命令

### 4.4 架构禁令

- 🚫 绝不能引入重量级中间件，例如 Kafka、Temporal、Camunda
- 🚫 MVP 阶段工作流引擎仅需轻量级状态机
- 🚫 绝不能为了简单需求引入复杂设计模式
- 🚫 绝不能为了未来可扩展，提前抽象三层空接口

### 4.5 编码风格禁令

- 🚫 绝不能为了表现“工程感”拆出大量无意义的 `Helper` / `Utils`
- 🚫 绝不能把简单逻辑过度包装成复杂抽象
- 🚫 绝不能擅自引入本项目未约定的新框架

---

## 5. 文件修改范围控制

在实施任务前，必须先说明预计会修改哪些文件。

### 5.1 默认原则

- 优先在已有模块内修改
- 能局部改，不要全局改
- 能补齐，不要推倒重来
- 能保持接口稳定，就不要顺手改协议

### 5.2 禁止顺手优化

如果当前任务是修复或实现某个明确功能：

- 🚫 不要顺手重构无关模块
- 🚫 不要顺手统一风格整个目录
- 🚫 不要顺手替换技术方案
- 🚫 不要顺手清理大量“看起来不顺眼”的代码

除非开发者明确要求“允许顺手重构”。

---

## 6. 目录职责与分层约定

### 6.1 顶层目录职责

- `api/`：后端服务、领域模型、执行引擎、Provider 抽象、持久化
- `web/`：前端应用、工作流编辑 UI、调试 UI、运行态展示
- `docs/`：阶段计划、设计说明、验收材料
- `scripts/`：辅助脚本，仅在确有必要时引入

### 6.2 后端分层约束

后端默认按以下责任分层：

- `controller`：仅负责入参接收、响应返回、状态码与错误边界
- `service`：负责业务编排与应用服务逻辑
- `domain`：负责核心领域模型与业务语义
- `repository` / `entity`：负责持久化映射
- `provider`：负责外部能力适配，例如 LLM / TTS

约束：

- Controller 不写复杂业务逻辑
- Repository 不承载业务规则
- Provider 适配层不反向污染核心领域层

### 6.3 前端分层约束

前端默认按以下责任分层：

- 页面负责组织界面
- 组件负责局部 UI 表达
- `api/` 负责请求封装
- `types/` 负责类型定义
- `utils/` 只保留真正可复用的纯函数

约束：

- 组件内部优先保持状态简单
- 避免把复杂业务规则散落在多个组件里
- 尽量通过显式类型与早返回降低嵌套复杂度

---

## 7. 代码与注释规范：Show, Don’t Tell

### 7.1 高质量业务注释

要求：必须写业务注释，但绝不写解释代码表面字面意思的废话。  
注释必须解释代码背后的**业务意图（Why）**、**边界条件**或**复杂算法思路**。

#### ❌ 错误示范

```java
// 获取用户状态
String status = node.getStatus();

// 如果是挂起状态
if ("SUSPENDED".equals(status)) {
    // 返回
    return;
}
```

#### ✅ 正确示范

```java
// 第一版 MVP UI 暂不开放人工恢复挂起操作，因此遇到 SUSPENDED 状态直接拦截，避免引擎死锁
if ("SUSPENDED".equals(node.getStatus())) {
    return;
}
```

### 7.2 Java DTO 规范

针对不可变数据载体，严格优先使用 **Java record**，不要写传统的带 Lombok 的 DTO Class。

#### ❌ 错误示范

```java
@Data
public class LlmResponse {
    private String content;
}
```

#### ✅ 正确示范

```java
public record LlmResponse(String content) {}
```

### 7.3 构造器注入

Spring Boot 组件强制使用**构造器注入**。  
允许配合 Lombok `@RequiredArgsConstructor` 使用。  
绝对禁止使用 `@Autowired` 字段注入。

### 7.4 前端 TypeScript 规范：早期返回与扁平化

要求：使用 Early Return 减少 `if/else` 嵌套层级。

#### ❌ 错误示范

```ts
if (node) {
  if (node.data) {
    process(node.data);
  }
}
```

#### ✅ 正确示范

```ts
if (!node?.data) return;

process(node.data);
```

### 7.5 时间处理规范

- 前端强制使用 `dayjs`
- 后端强制使用 `java.time.LocalDateTime`
- 禁止引入混乱的时间表示方式

---

## 8. 测试、验证与交付方式

### 8.1 测试执行原则

AI 不负责声称测试通过。  
AI 只负责：

- 明确指出建议执行哪些验证
- 说明预期结果
- 根据开发者反馈继续修正

### 8.2 每次修改后必须提供

每次完成一阶段编码后，必须输出：

1. 修改了哪些文件
2. 核心改动点
3. 为什么这样改
4. 开发者下一步应执行的验证命令
5. 预期现象
6. 潜在风险或遗留问题

---

## 9. 阶段性交付标准

每个阶段完成时，必须说明是否满足以下条件：

- 范围是否闭合
- 是否引入了额外复杂度
- 是否改动了未授权的模块
- 是否更新了相关文档
- 是否给出了可执行验证步骤
- 是否还有必须显式确认的风险点

如果某阶段未闭合，必须明确指出，不允许伪装成“已完成”。

---

## 10. Worktree 并行开发约束

本项目允许并行开发，但必须遵守以下规则：

- 一个 worktree 只处理一个明确主题
- 不同阶段 / 主题不要混在同一个 worktree
- 未经确认，不要跨多个主题同时修改
- 并行开发时，必须在计划中标明“当前分支 / 当前主题 / 不涉及范围”

建议主题粒度示例：

- `phase-1-backend-foundation`
- `phase-2-frontend-foundation`
- `fix-runtime-state-machine`
- `docs-acceptance-materials`

---

## 11. 常见反复犯错点：Bug 补丁区

> 日常留空。当开发者发现 AI 在本项目反复犯错时，补充在此作为创可贴规则。

- 依赖注入：Spring Boot 组件强制使用构造器注入，绝对禁止字段注入
- 时间处理：前端强制使用 `dayjs`，后端强制使用 `java.time.LocalDateTime`

---

## 12. 架构混淆捕获：AI 反馈通道

如果你在梳理或修改本项目代码时，特别是以下模块：

- 流图引擎机制
- Provider 抽象层
- 执行状态流转
- 前后端契约边界

遇到了任何让你惊讶、困惑、缺乏上下文、容易误改或高风险的架构设计，请主动在本章节**追加记录**：

1. 发现了什么问题
2. 为什么它危险
3. 容易导致什么误改
4. 建议后续如何收敛

> 只能追加记录，不能覆盖已有规则。  
> 这相当于向开发者发出“架构重构预警”。

---

## 13. AI 输出风格要求

- 保持克制，避免自我发挥
- 不要写空泛大词
- 不要假装完成了未验证的事情
- 不要一次输出超大段无分层代码
- 先计划，再实现，再汇报
- 多展示结构化判断，少堆砌口号

---

## 14. 文档更新规则

- 未经明确要求，不要创建新文档
- 优先更新已有文档，而不是新增 `PLAN.md`、`TODO.md`、`NOTES.md` 一类文件
- 如确需新建文档，必须先说明用途并获得确认

---

## 15. 建议验证命令清单

> AI 可以建议开发者执行以下命令，但不得自行执行完整测试套件并声称结果通过。

### 后端

```bash
cd api
./mvnw spring-boot:run
```

```bash
cd api
./mvnw test
```

### 前端

```bash
cd web
pnpm install
pnpm dev
```

```bash
cd web
pnpm test
```

```bash
cd web
pnpm build
```

### 全项目

```bash
docker compose up --build
```

---

## 16. 完成定义

除非开发者明确降低要求，否则每个阶段的完成标准为：

- 功能范围与计划一致
- 没有明显越界改动
- 没有引入未授权技术栈
- 给出变更说明
- 给出验证命令
- 给出已知风险
- 文档与实现保持一致